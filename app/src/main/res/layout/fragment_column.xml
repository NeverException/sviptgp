<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/fragment_header_height"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:background="@color/color_white">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="栏目"
            android:gravity="center"
            android:textSize="@dimen/fragment_header_text_size"/>

        <ImageButton
            android:id="@+id/column_search"
            android:layout_width="@dimen/search_image_dimens"
            android:layout_height="@dimen/search_image_dimens"
            android:background="@drawable/search_image_selector"
            android:layout_gravity="end|center_vertical"/>

    </FrameLayout>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/column_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="3dp"
        android:background="@color/column_item_text_color"/>

</LinearLayout>